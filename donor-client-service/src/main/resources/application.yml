spring:
  application:                                      
    name: donor-service-client
    
server:
  port: 7010
    
 
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka   